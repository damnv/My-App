### Elife 

## Build Setup

```bash
# install dependencies
$ composer install 
$ npm install

# serve with hot reload at 127.0.0.1
$ php artisan serve

# track file changes in vuejs
$ npm run watch

# migration database
$ php artisan migrate

# seed database
$ php artisan db:seed
